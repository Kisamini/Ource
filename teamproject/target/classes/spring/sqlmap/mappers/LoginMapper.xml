<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.ource.login.service.impl.LoginMapper">

	<select id="userLogin" parameterType="memberVO" resultType="int">
		SELECT COUNT(*) FROM USERS WHERE USER_ID = #{userid} AND USER_PW = #{passwd} 
	</select>
	
	<select id="selectUser" parameterType="memberVO" resultType="memberVO">
	   SELECT USER_NAME name, USER_ID userid, USER_BIRTH birth, USER_ANSWER answer FROM USERS WHERE USER_ID = #{userid} 
	</select>
	
	<select id="selectUser2" parameterType="memberVO" resultType="memberVO">
		SELECT USER_NAME name, USER_ID userid, USER_BIRTH birth, USER_ANSWER answer FROM USERS WHERE USER_NAME = #{name}
	</select>
	
	<select id="searchId" parameterType="memberVO" resultType="int">
	   SELECT COUNT(*) FROM USERS WHERE USER_NAME = #{name} AND USER_BIRTH = #{birth}
	</select>
	
</mapper>